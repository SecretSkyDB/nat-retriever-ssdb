; Restated from [tool.pytest.ini_options] in pyproject.toml so pytest 9
; honors it even when invoked from a parent directory whose own pytest.ini
; would otherwise win the rootdir search.
[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::pytest.PytestConfigWarning
